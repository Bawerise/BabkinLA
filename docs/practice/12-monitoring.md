# 12) Мониторинг

1. Соберите накопительную статистику обращений к таблице
    - создайте новую базу данных
    - создайте в ней таблицу с единственной колонкой типа `NUMERIC`
    - вставьте в таблицу несколько сотен строк
    - удалите все строки
    - проверьте статистику обращений к таблице (запросом к `pg_stat_all_tables`)
    - выполните очистку таблицы
    - повторно проверьте статистику обращений
2. Используйте расширение `pg_stat_statements`
    - установите параметр `shared_preload_libraries` (`ALTER SYSTEM SET shared_preload_libraries = 'pg_stat_statements';`)
    - перезапустите сервис db
    - установите расширение (`CREATE EXTENSION pg_stat_statements;`)
    - выполните несколько однотипных запросов `INSERT` в созданной ранее таблице
    - с помощью запроса к представлению `pg_stat_statements` покажите статистику по самым длительным запросам
    ```sql
    SELECT query, calls, total_exec_time
    FROM pg_stat_statements
    ORDER BY total_exec_time DESC
    FETCH FIRST 1 ROWS ONLY;
    ```
    - сбросьте параметр `shared_preload_libraries`

# Содержимое пояснительной записки
1. Скриншоты с введенными командами, результатами их выполнения и развернутые комментарии к ним